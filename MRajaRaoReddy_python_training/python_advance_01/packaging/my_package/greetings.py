# This module contains a function to greet a user by their name.

def greet_user(name: str) -> str:
    """
    Args:
        name: User name.

    Returns:
        str: Greeting message.
    """
    return f"Hello, {name}!"